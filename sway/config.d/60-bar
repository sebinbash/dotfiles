### Status bar

# Use waybar
# config in: ~/.config/waybar
bar {
	swaybar_command waybar
}

exec_always nm-applet --indicator
exec_always dbus-update-activation-environment DISPLAY
exec_always blueman-applet

